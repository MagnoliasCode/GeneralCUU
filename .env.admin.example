# Solo para los scripts de administración (seed / create-admins).
# NUNCA subas el archivo .env.admin real a git — usa la Service Role Key,
# que tiene permisos totales sobre tu proyecto Supabase.
# La encuentras en: Supabase Dashboard > Project Settings > API > service_role key.

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
